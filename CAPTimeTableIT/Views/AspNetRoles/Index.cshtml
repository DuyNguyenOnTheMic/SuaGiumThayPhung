@model List<CAPTimeTableIT.Infrastructure.Entities.ApplicationRole>
@*@model IEnumerable<CAPTimeTableIT.Infrastructure.Models.UserRoleModel>*@

@{
    ViewBag.Title = "Danh sách vai trò";
}


    <div class="main-container">
        <div class="pd-ltr-20 xs-pd-20-10">
            <div class="min-height-200px">
                <div class="page-header">
                    <div class="row">
                        <div class="col-md-6 col-sm-12">
                            <nav aria-label="breadcrumb" role="navigation">
                                <ol class="breadcrumb">
                                    <li class="breadcrumb-item"><a href=@Url.Action("Index", "AspNetRoles")>Trang chủ</a></li>
                                    <li class="breadcrumb-item active" aria-current="page">Vai trò</li>
                                </ol>
                            </nav>
                        </div>
                    </div>
                </div>
                <!-- Export Datatable start -->
                <div class="card-box mb-30">
                    <div class="pd-20">
                        <p>
                            <a class="btn btn-primary" href="@Url.Action("Create")">Thêm mới</a>
                        </p>
                    </div>
                    <div class="pb-20">
                        <table class="table hover multiple-select-row data-table-export nowrap">
                            <thead>
                                <tr>
                                    <th class="table-plus datatable-nosort">Role</th>
                                    @*<th>User</th>*@
                                    <th>Hành động</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var role in Model)
                                {
                                    <!--<tr>
                                    <td>
                                        role.Name
                                    </td>-->
                                    @*<td></td>*@
                                    <!--<td>-->
                                    @*<a href="@Url.Action("Edit", new { id = role.Id })">Edit</a> |
                                        <a href="@Url.Action("Delete", new { id = role.Id })">Delete</a> |
                                        <a href="@Url.Action("Create", "AspNetUserRole", new { id = role.Id })">Add User</a>*@
                                    <!--</td>
                                    </tr>-->
                                    @*foreach (var user in role.UserRoles)
                                        {
                                            <tr>
                                                <td></td>
                                                <td>@user.User.UserName</td>
                                            </tr>
                                        }*@
                                       @* foreach (var user in role.)
                                        {*@
                                            <tr>
                                                <td>@role.Name</td>
                                                <td>
                                                    <a title="Xóa tài khoản" href="@Url.Action("Delete", "AspNetRole", new {id = role.Id })" class="btn btn-danger btn-circle">
                                                        <i class="fa fa-times"></i>
                                                    </a>
                                                    <a href="@Url.Action("Create", "AspNetUserRole", new { id = role.Id })">Add User</a>
                                                </td>
                                            </tr>
                                        @*}*@
                                    }
                            </tbody>
                        </table>
                    </div>
                </div>
                <!-- Export Datatable End -->
            </div>
        </div>
    </div>

